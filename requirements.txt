# RunPod SDK
runpod>=1.9.0

# HTTP — used by handler to talk to ComfyUI over localhost
requests

# Image I/O — decoding the base64 input keyframe
Pillow

# Video encoding (fallback utility; primary output comes from ComfyUI/VHS)
imageio[ffmpeg]>=2.34

# numpy pinned <2 for broad compatibility with ComfyUI ecosystem packages
numpy<2
